import 'package:flutter/material.dart';

class BakerProfileScreen extends StatelessWidget {
  const BakerProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('baker profile page'),
      ),
    );
  }
}
