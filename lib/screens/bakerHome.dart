import 'package:flutter/material.dart';

class BakerHomeScreen extends StatelessWidget {
  const BakerHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('baker home page'),
      ),
    );
  }
}
