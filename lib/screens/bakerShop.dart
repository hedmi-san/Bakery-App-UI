import 'package:flutter/material.dart';

class BakerShopScreen extends StatelessWidget {
  const BakerShopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('baker shop page'),
      ),
    );
  }
}
